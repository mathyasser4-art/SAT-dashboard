## Debug Session: 20260403_172439_216248
**Problem:** "Failed to fetch" on all update/add/delete operations in the deployed app.

## Root Causes Found

### 1. `updateSubject.api.js` — hardcoded localhost URL
- **File:** `src/api/updateSubject.api.js`
- **Change:** `http://localhost:8000/subject/updateSubject` → `/api/subject/updateSubject`
- **Why:** Dev URL was committed by mistake, causing all subject updates to fail with "Failed to fetch" in production.

### 2. Backend CORS preflight (OPTIONS) returns HTTP 500
- **Evidence:** `curl -X OPTIONS https://sat-backend-production.up.railway.app/system/updateSystem/test` → `HTTP/1.1 500`
- **Impact:** Browser blocks ALL PUT/POST/DELETE/PATCH mutations from the Vercel frontend because the preflight check fails. GET requests (no body → no preflight) still worked.
- **Fix:** Introduced a Vercel server-side proxy (`vercel.json` rewrites) and a CRA dev proxy (`src/setupProxy.js`) so all API calls go through the same origin — bypassing CORS entirely.

## Changes Made

### 1. `vercel.json` — Added Vercel proxy rewrite
- **Change:** Added `rewrites` block: `/api/:path*` → `https://sat-backend-production.up.railway.app/:path*`
- **Revert:** Remove the `"rewrites"` key from vercel.json

### 2. `src/setupProxy.js` — NEW FILE — CRA dev proxy
- **Change:** Created proxy middleware using `http-proxy-middleware` to forward `/api/*` to the backend with path rewrite (strips `/api` prefix) for local development.
- **Revert:** Delete `src/setupProxy.js`

### 3. All 30 API files — Changed absolute backend URLs to relative `/api/` paths
- **Files:** All files under `src/api/`
- **Change:** `https://sat-backend-production.up.railway.app/...` → `/api/...` (and `http://localhost:8000/...` → `/api/...` for updateSubject)
- **Revert:** Replace `/api/` with `https://sat-backend-production.up.railway.app/` in each file (and `http://localhost:8000/` for updateSubject.api.js)

## Revert Status
- [ ] Change 1 — vercel.json rewrites
- [ ] Change 2 — src/setupProxy.js (delete file)
- [ ] Change 3 — All 30 API files (restore absolute URLs)
